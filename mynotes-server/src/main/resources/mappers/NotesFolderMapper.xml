<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.fslt.dao.NotesFolderDao">

    <sql id="allFolder">
        ${table}.id AS id
        ,${table}.parent_id AS parentId
        ,${table}.folder_name AS folderName
        ,${table}.user_code AS userCode
        ,${table}.user_id AS userId
        ,${table}.create_time AS createTime
        ,${table}.modify_time AS modifyTime
        ,${table}.delete_flag AS deleteFlag
        ,${table}.status AS status
    </sql>

    <select id="getFolderList"
            resultType="com.fslt.entity.pojo.Folder">
        SELECT
        <include refid="allFolder">
            <property name="table" value="nf"/>
        </include>
        FROM
        t_notes_folder nf

    </select>


</mapper>